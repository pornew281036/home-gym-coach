# จัด GitHub Pages — ทีละขั้น (ไม่ต้องเขียนโค้ด)

เป้าหมาย: ได้ลิงก์ถาวร เปิดบนมือถือได้โดย **ไม่ต้องเปิด Mac**

ตัวอย่างลิงก์สุดท้าย:

`https://ชื่อผู้ใช้GitHubของคุณ.github.io/home-gym-coach/`

---

## สิ่งที่ต้องมี

1. บัญชี [GitHub](https://github.com/signup) (สมัครฟรี)
2. โฟลเดอร์แอปบน Mac:  
   `Desktop/AI Workspace/01_โครงการที่กำลังทำ/home-gym-coach`

---

## ขั้นที่ 1 — สร้างที่เก็บบน GitHub

1. เปิด https://github.com/new
2. Repository name: พิมพ์ `home-gym-coach`
3. เลือก **Public** (ฟรีสำหรับ Pages)
4. **ไม่ต้อง**ติ๊ก Add README
5. กด **Create repository**

---

## ขั้นที่ 2 — อัปโหลดไฟล์แอป (วิธีง่ายสุด)

1. ในหน้า repo ใหม่ กด **uploading an existing file**
2. เปิด Finder ไปโฟลเดอร์ `home-gym-coach`
3. ลากไฟล์/โฟลเดอร์เหล่านี้ใส่หน้าเว็บ GitHub:

   - `index.html`
   - `manifest.json`
   - `sw.js`
   - `.nojekyll` (สำคัญ — ถ้าไม่เห็นใน Finder กด Cmd+Shift+. เพื่อโชว์ไฟล์ซ่อน)
   - โฟลเดอร์ `css` ทั้งโฟลเดอร์
   - โฟลเดอร์ `js` ทั้งโฟลเดอร์
   - โฟลเดอร์ `assets` ทั้งโฟลเดอร์
   - โฟลเดอร์ `icons` ทั้งโฟลเดอร์

   ไม่บังคับ: `start_server.sh`, `คู่มือใช้งาน.md`

4. ด้านล่างกด **Commit changes**

---

## ขั้นที่ 3 — เปิด Pages

1. ใน repo กดแท็บ **Settings**
2. เมนูซ้าย **Pages**
3. Build and deployment → Source เลือก **Deploy from a branch**
4. Branch เลือก `main` (หรือ `master`) โฟลเดอร์ `/ (root)`
5. กด **Save**
6. รอ 1–3 นาที แล้วรีเฟรชหน้า Pages จะขึ้นลิงก์สีเขียว

ลิงก์จะเป็นประมาณ:

`https://YOUR_USERNAME.github.io/home-gym-coach/`

---

## ขั้นที่ 4 — มือถือ

1. เปิดลิงก์นั้นใน Chrome บนมือถือ
2. เมนู ⋮ → **Add to Home screen**
3. ครั้งถัดไปเปิดจากไอคอนหน้าโฮม — **ไม่ต้องเปิด Mac**

ข้อมูลยก/Fitdays ยังเก็บในมือถือเครื่องนั้น (เหมือนเดิม)  
แนะนำ: ตั้งค่า → ส่งออก JSON สำรองทุกสัปดาห์

---

## แก้แอปทีหลัง

แก้ไฟล์บน Mac แล้วอัปโหลดทับไฟล์เดิมบน GitHub (หรือลากไฟล์ที่แก้ขึ้นไป Commit อีกครั้ง)  
รอประมาณ 1 นาที แล้วรีเฟรชมือถือ (บางทีต้องปิดแท็บเก่าแล้วเปิดใหม่)

---

## ถ้าติดปัญหา

| อาการ | ทำไง |
|--------|------|
| หน้า 404 | รออีก 2 นาที / เช็กว่ามี `index.html` ที่ราก repo |
| CSS/JS ไม่ขึ้น | เช็กว่าอัปโฟลเดอร์ `css` และ `js` ครบ |
| ไม่เจอ `.nojekyll` | ใน Finder กด Cmd+Shift+. แล้วอัปไฟล์นี้ด้วย |
| อยากให้ช่วยอัปแทน | บอก **ชื่อผู้ใช้ GitHub** แล้วอนุญาตติดตั้ง GitHub CLI / login |

---

## เทียบสั้น ๆ อีกครั้ง

- **ก่อนหน้า:** มือถือ → Mac ต้องเปิด → ลิงก์ IP เปลี่ยนได้  
- **หลัง Pages:** มือถือ → เน็ต → ลิงก์เดิม → Mac ปิดได้
